# 故障排查

这里记录常见问题和排查方式。

## 修改开关后没有立即生效

开关保存后会主动同步已打开的 B 站标签页。页面内的隐藏、恢复和搜索页重定向通常会即时生效。

如果仍没有变化，按顺序尝试：

1. 刷新当前 B 站页面。
2. 关闭并重新打开扩展 popup，确认开关状态已经保存。
3. 打开完整设置页，确认模块状态正确。
4. 在浏览器扩展管理页点击“重新加载”。

说明：网络拦截类模块会立刻影响后续请求，但已经被拦截、没有加载到页面里的内容，可能需要页面重新请求后才会重新出现。

## 视频页某个正常功能被隐藏

可以逐个关闭以下模块定位：

- 隐藏热榜和分享入口
- 隐藏顶部导航
- 隐藏右侧推荐
- 隐藏评论区
- 隐藏弹幕
- 隐藏广告和推广
- 隐藏播放结束推荐
- 禁用自动连播

定位到具体模块后，再调整选择器或隐藏策略。

## 视频页评论区仍然可见

确认：

- 扩展总开关已开启。
- 当前不是白名单页面。
- “隐藏评论区”模块已开启。
- 页面已经刷新。

B 站评论区 DOM 经常变化。如果出现漏网结构，可以在测试里补一个模拟结构，然后扩展 `content.js` 中的评论选择器或文本兜底逻辑。

## 搜索功能不可用

确认：

- “开启搜索功能”模块已开启。
- 当前页面没有命中白名单后的旧样式缓存。
- 页面已经刷新。

搜索功能现在是独立开关。关闭“隐藏热榜和分享入口”只会影响热搜词、视频标签和分享扩散入口，不应该隐藏搜索栏或阻止进入搜索结果页。

如果开启后仍然无法搜索，确认地址栏最终是否停留在 `search.bilibili.com`。如果仍被带回关注动态页，通常说明扩展尚未重新加载到最新版本，或“开启搜索功能”开关没有保存成功。

## 视频仍然自动播放下一个内容

确认：

- “禁用自动连播”模块已开启。
- 页面已经刷新。
- 播放器里没有手动重新打开自动连播。

当前扩展会关闭原生 `video.autoplay`，并隐藏“自动连播”“自动开播”“下一个”等入口。如果 B 站更新了播放器控件名称，需要补充新的选择器或文本兜底。

## 个人空间没有被清理

确认：

- 扩展版本已经包含 `space.bilibili.com` 的 content script 匹配。
- 当前不是白名单页面。
- “隐藏热榜和分享入口”“隐藏右侧推荐”“隐藏广告和推广”“隐藏顶部导航”等模块已开启。
- 页面已经刷新。

个人空间应保留 UP 信息和视频入口，同时隐藏关注/粉丝关系入口、追番追剧、直播间和游戏推广。

## 首页没有跳转到关注动态

确认：

- 扩展总开关已开启。
- “首页跳到关注动态”模块已开启。
- 当前页面没有命中白名单。
- 当前页面确实是 `www.bilibili.com` 首页或分区页。

如果刚改过开关或白名单，刷新页面后再试。

## 不想让某个页面被处理

把页面加入白名单。

打开完整设置页，在“页面白名单”中每行填写一个 URL 或通配符，例如：

```text
https://www.bilibili.com/
https://www.bilibili.com/v/popular
https://www.bilibili.com/video/BV*/
```

保存后刷新目标页面。

## 白名单没有生效

确认：

- 每行只写一个 URL 或通配符。
- URL 包含协议，例如 `https://`。
- 保存白名单后刷新目标页面。
- 没有多余空格。

匹配规则：

- 没有 `*` 的条目按 URL 前缀匹配。
- 包含 `*` 的条目按通配符匹配。

## popup 或设置页显示异常

尝试：

1. 在扩展管理页重新加载扩展。
2. 关闭并重新打开 popup。
3. 打开完整设置页检查配置是否正常。
4. 运行自动回归测试确认扩展是否仍能正常加载。

## 自动回归测试失败

运行：

```bash
node scripts/regression-test.js
```

如果提示找不到浏览器，可以指定浏览器路径：

```bash
set BILIBILI_LESS_BROWSER=C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe
node scripts\regression-test.js
```

如果失败发生在视频页加载阶段，可能是默认测试视频临时不可访问。可以换一个视频：

```bash
set BILIBILI_LESS_TEST_VIDEO=https://www.bilibili.com/video/你的BV号
node scripts\regression-test.js
```

## 自动测试会影响日常浏览器吗

不会。

测试脚本会复制一份临时扩展目录，并启动独立的 Edge/Chromium 测试配置。测试结束后会关闭测试浏览器并清理临时目录。
