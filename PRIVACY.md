# 隐私说明

Less BiliBili 是一个本地运行的浏览器扩展。它的作用是在 B 站页面中隐藏干扰元素，并使用浏览器内置能力拦截部分推荐和广告接口。

扩展不会收集、上传或分析用户数据。

## 不会收集的数据

扩展不会收集：

- 浏览记录
- 搜索记录
- 账号信息
- 密码或登录凭据
- 页面内容
- 观看记录
- 个人身份信息

扩展不会使用远程统计、埋点或分析服务。

## 本地保存的数据

扩展会使用浏览器本地存储保存配置：

- 总开关状态
- 模式预设
- 模块开关
- 页面白名单

这些数据只保存在浏览器本地的扩展存储中，不会发送给扩展作者或第三方服务。

## 使用的权限

| 权限 | 用途 |
| --- | --- |
| `storage` | 在本地保存扩展配置 |
| `declarativeNetRequest` | 使用浏览器内置规则拦截推荐、广告等接口 |
| `webNavigation` | 在首页重定向模块开启时，将非允许页面带回关注动态 |
| `*://*.bilibili.com/*` | 在 B 站页面中注入脚本并隐藏干扰元素 |

## 网络拦截说明

扩展使用 Manifest V3 的 `declarativeNetRequest`。

这些规则由浏览器执行，规则文件随扩展打包，不需要把 URL 或页面内容发送到外部服务器。

当前网络规则分为：

- 推荐内容规则
- 动态页和热搜规则
- 广告推广规则

这些规则会随对应模块开关启用或停用。

## 白名单说明

页面白名单也保存在浏览器本地。

白名单只用于判断某个 B 站页面是否应该放行，不会上传，也不会用于任何统计分析。
