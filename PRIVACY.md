# 隐私说明

Less BiliBili 是一个本地运行的浏览器扩展。它在 B 站页面中隐藏干扰元素，并使用浏览器内置的 Manifest V3 `declarativeNetRequest` 能力拦截部分推荐、探索和广告接口。

扩展不会收集、上传或分析用户数据。

## 不会收集的数据

扩展不会收集：

- 浏览记录
- 搜索记录
- 账号信息
- 密码或登录凭据
- 页面正文内容
- 观看记录
- 个人身份信息
- 评论、弹幕或私信内容

扩展不会使用远程统计、埋点或分析服务。

## 本地保存的数据

扩展会使用浏览器本地扩展存储保存配置：

- 总开关状态
- 模式预设
- 模块开关
- 页面白名单

这些数据只保存在浏览器本地的扩展存储中，不会发送给扩展作者或第三方服务。

## 使用的权限

| 权限 | 用途 |
| --- | --- |
| `storage` | 在本地保存扩展配置 |
| `declarativeNetRequest` | 使用浏览器内置规则拦截推荐、探索、动态和广告接口 |
| `webNavigation` | 在首页重定向模块开启时，将非允许页面带回关注动态 |
| `*://*.bilibili.com/*` | 在 B 站页面中注入脚本并隐藏干扰元素 |

## 网络拦截说明

扩展使用 Manifest V3 的 `declarativeNetRequest`。

规则由浏览器执行，规则文件随扩展打包，不需要把 URL 或页面内容发送到外部服务器。

当前网络规则分为：

- 推荐内容规则：`rules_recommendations.json`
- 探索入口规则：`rules_exploration.json`
- 动态页规则：`rules_dynamic.json`
- 广告推广规则：`rules_ads.json`

这些规则会随对应模块开关启用或停用。

## 白名单说明

页面白名单也保存在浏览器本地。

白名单只用于判断某个 B 站页面是否应该放行，不会上报，也不会用于任何统计分析。

匹配白名单后：

- 首页重定向不会处理该页面。
- 内容脚本不会清理该页面。

## 第三方服务

扩展本身不调用第三方服务。

用户访问 B 站页面时，B 站自身仍会按其网站逻辑加载资源、处理登录状态和记录行为。Less BiliBili 只能控制扩展自身的行为，不能替代 B 站的隐私政策。
